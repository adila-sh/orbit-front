# Git hooks. Installed by `bun run prepare` (runs on `bun install`).
# See https://lefthook.dev/configuration/
pre-commit:
  parallel: true
  commands:
    lint:
      run: bun run lint
    format:
      run: bun run format:check
    typecheck:
      run: bun run typecheck
